<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>

    <!-- Include Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

    <!-- Optional Bootstrap JS + jQuery (if you want to use Bootstrap's JavaScript components) -->


    <style>
        /* Custom CSS */
        body {
            /* Gradient background */
            background: linear-gradient(45deg, #f2f4f8, #dde1e7);
            height: 100vh;
            display: flex;
            align-items: center; /* center the content vertically */
            justify-content: center; /* center the content horizontally */
        }
        h1 {
            color: #2c3e50; /* Dark shade for the heading */
            margin-bottom: 20px;
        }

        label {
            color: blue; /* Different shade for labels */
        }

        .form-container {
            background: #ffffff; /* White background for form container */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.1); /* subtle shadow for depth */
        }

    </style>
</head>
<body>

<div class="container mt-5 form-container">
    <h1 class="text-center">Login</h1>
    <form action="/login" method="POST" class="mt-4">
        <div class="form-group">
            <label for="mail">Email :</label>
            <input type="email" name="email" id="mail" class="form-control">
        </div>
        <div class="form-group">
            <label for="password">Password :</label>
            <input type="password" name="password" id="pwd" class="form-control">
        </div>
        <button type="submit" class="btn btn-primary">Login</button>
    </form>
</div>

</body>
</html>
